# KNOWN_LIMITATIONS.md — HeteroSense-FL v2.0

構造的限界の恒久記録。使用範囲を誤ると結論が無効になるため、実験設計時に必読。

## KL-1 (2026-07-05) — 真値が PIR 発火時刻へ決定的に漏洩する（timing 学習上限の評価に使用不可）

**発見（研究D 前提監査, AUDIT_D3）**: v2.0 の行動生成 `heterosense/_core/_b2t.py::generate_b2t_night`
は真の離床時刻 `exit_s` に対し
```
motion[exit_s : exit_s + mv] = True     # mv=4s のバースト、開始 = exit_s
truth.append((exit_s, exit_s + dur_s))  # ground-truth ラベル = exit_s（同一秒）
```
とし、`_pir_model.py::observe_sequence` は各 moving 秒で（不応期外なら）**その整数秒に発火**、
report_period で floor 量子化するのみ。在床フリッカは ~150s 間隔・不応期 5–30s なので exit 直前は
静かで不応期は exit 発火を抑制しない。結果、**真の exit ラベル秒に発火が決定的に立つ**。

**実測確認**（pir_only 24軒, exit 最近傍発火距離, report_period 別）:
report_period=0 → **中央値 0.00s**（ラベル秒に厳密一致）／30s → 13s／60s → 22s＝**量子化で完全説明**。
すなわち発火タイミングは真値の決定的変換（＋量子化）であり、**真の情報破壊ではない**。

### ★使用不可の範囲
- **observation-model 学習の timing 上限の評価**（「学習した観測モデルが exit 時刻をどこまで精密に
  復元できるか」）。真値が発火列に決定的に漏れるため、十分表現力のあるモデルは**シミュレータを
  逆変換**して見かけ上の高精度を出す。この上限は実世界の検出可能性を反映しない。
  → timing 精度・その系の下流 cost の**絶対値**を実性能主張に使ってはならない。

### 使用可能な範囲（KL-1 の影響を受けない）
1. **検出可能性（anchor）**: 単一PIR が sub-minute B2T を観測**できない**という定性事実
   （抽出器 recall 0.179, 61軒 0.03–0.55）。これは情報の有無であり timing 精度の学習上限ではない。
2. **下流遅延の系列比較**: モダリティ系列（PIR/PIR+圧力/LiDAR）間の cost 順位・検出可能性の対照。
   固定の観測モデル class 下での相対比較。
3. **FL 機構比較**: 同一 class 下で fedavg vs modality_group、missing-support 掃引の**相対**効果。
4. **増幅則（timing→cost の関係）と恒等式の床**: 下流 cost 関数（t* 分位点＋miss 崖）の性質であり、
   timing がどの値でも cost がどう応答するかの**関係**。timing の低値を学習で出せるかとは独立。

**要点**: 「相対比較・機構・検出可能性・関係」は可、「学習した timing 精度の絶対上限」は不可。

---

## KL-1 適合性照合 — 旗艦論文の各実験（監査タスク2, 全 PASS）

旗艦 "The Price of Imperfect Observation" の4実験が KL-1 の使用可能範囲内かを1点ずつ照合。
**どれも「timing 学習上限の評価」に依存しない**ことを確認（依存が見つかれば旗艦の監査事案となるが、無し）。

| 実験 | 依存の有無 | 判定・理由 |
|---|---|---|
| **増幅則 1.7min/s** | timing→cost の**関係**を使用。timing 値は**固定 logistic** の実測誤差（73.5s 等、上限でない） | ✅可。関係は下流 cost 関数の性質。低 timing 端（LiDAR 0.083）は下記注。|
| **type>count（112 vs 0.2分）** | モダリティ**系列比較＋検出可能性**（単一PIR は exit を miss=recall 0.177） | ✅可。系列比較・anchor。単一PIR の**改善余地**は主張していない（それは研究D=凍結）。|
| **床（LiDAR 0.08分）** | **恒等式**（観測を良くしても cost は t* 分位点で下げ止まる） | ✅可。下記注（0.08 は LiDAR モデルの残差 timing でもある）。|
| **FL 比較（78.2%/崩壊回避, p=0.032/…）** | 同一 class 下の**機構比較**（missing-support で群別が勝つ） | ✅可。相対・機構。|

**注（2件・実性能主張でないため PASS 内だが記録）**:
- 増幅則の x軸最低点（LiDAR timing 0.083分）は観測モデルの忠実度に依存。ただし増幅則の主張は
  timing→cost の**関係**であり、PIR/圧力点だけでも関係は追える。低 timing 端の絶対値は主張の核でない。
- 床の 0.08分は「LiDAR 学習モデルの残差 timing」でもある（真のオラクルなら 0）。旗艦は 0.08 を
  「分位点の床」と説明するが、厳密には近似（LiDAR は noisy な per-step draw で PIR のような決定的
  ラベル漏洩は無いため KL-1 非該当）。主張（良い観測でも cost は 0 に落ちない＝恒等式）は不変。

**結論**: 旗艦の4実験は KL-1 の使用可能範囲に収まる。KL-1 が無効化する「学習 timing 上限」に
依存するのは研究D（D-2/D-3/D-4）のみで、これは既に起草凍結（AUDIT_D3, 裁定3）。旗艦は健全。

---

## KL-1 の外部実データによる裏書き（EXT-1 / TIHM, 2026-07-05・実データ一点）
`longlie_study/ext_validation/REPORT_EXT1.md`。公開実データ TIHM（CC BY 4.0, 床下ベッドセンサ＋
Bedroom PIR）で、ベッドセンサ由来の夜間離床時刻に対する **Bedroom-PIR 最近傍発火** を実測:
**中央値 50分（離床）> 36分（ランダム時刻）**＝**実 PIR は離床を標定しない（ランダムより遠い）**。
- これは sim の D-2（発火が exit を **7秒**で標定）と**正反対**であり、**KL-1（sim 自己反転）を実データ
  一点で裏書き**（＋CASAS 単一PIR anchor＝標定不可 とも一致）。
- caveats: TIHM の PIR は疎/粗い集約・多占有・n 小・**有意性なし**。物理検証の代替ではない。

### 研究D の解錠条件（更新・より狭く・より正直に）
旧: 「smart room 実 PIR 列で D-2 統計を再計算し量子化超の実信号が exit を標定すれば解錠」。
**新（EXT-1 を受けて厳格化）**: **smart room 実測が TIHM と＜逆＞の結果——すなわち実 PIR/在床センサに
exit-onset 標定情報が＜存在する＞こと（最近傍発火がランダムより有意に近い）——を示した場合にのみ**、
研究D（表現学習）の起草を解錠する。sim(D-2/D-3/D-4) と TIHM(実データ一点) が**共に否定的**な現状では、
既定は「exit-onset 情報は実世界の単一PIR には無い」＝研究D は凍結継続。解錠の立証責任は smart room 側。

---

## 修正計画（v2.1 で KL-1 を解消）: `docs/v2.1_determinism_fix.md` 参照
確率的 exit-motion ジッタ＋発火の確率的欠落で決定論を切る。**実装は smart room 実測誤差分布の
取得後**（実データで較正した確率モデルにするため）。それまで KL-1 の使用範囲を厳守。
