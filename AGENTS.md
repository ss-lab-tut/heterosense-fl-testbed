# プロジェクト概要

本リポジトリは、高齢者見守りシステムにおけるLiDARを用いた転倒検知の研究プロジェクトです。

本研究では、シミュレータ上で生成したLiDARデータとベッド圧センサを用いて転倒検知モデルを構築し、将来的に実環境への適用（Sim-to-Real）を目指します。

---

# 最重要ルール

## 再現性を絶対に壊さない

このシミュレータは、

> 「同じ設定、同じシード値なら誰が実行しても同じ出力になる」

ことを保証しています。

### 禁止事項

共有乱数生成器（rng）へ新しい乱数呼び出しを追加してはいけません。

新しい機能で乱数が必要な場合は、以下を利用してください。

- `rng.spawn()`
- 専用の子生成器

既存データの生成順序を変えないことを最優先にしてください。

---

## Git運用

`main`へ直接pushしないでください。

必ず`feature/`ブランチで作業してください。

変更後は、以下を確認してからcommitしてください。

- `pytest`
- 既存テスト
- 再現性

その後GitHubへpushし、Pull Request（PR）を作成してください。

---

## コード変更時

変更は小さく行ってください。

一度に大きく修正しないでください。

コードを書き始める前に、以下を確認してください。

- 現在のブランチ
- `git status`
- 関連コード

---

## 評価

Accuracyだけで性能を判断しないでください。

重要なのは以下です。

- Recall
- False Alerts
- 条件別性能

---

## 条件推定器

入力には現実で観測できる情報のみ使用してください。

使用可能例：

- `point_count`
- `z_mean`
- `z_std`
- baselineとの差分
- `pressure`
- `x_std`
- `y_std`
- `xy_spread`

使用禁止：

- シミュレータ内部の遮蔽パラメータ
- シミュレータ内部の設置条件
- 推論時に取得できない情報

---

## 実装前

コードを書く前に、以下を確認してください。

- `README.md`
- `RESEARCH_CONTEXT.md`
- `CURRENT_STATUS.md`

---

## 不明点

仕様が曖昧な場合は推測で実装しないでください。

必ず質問してください。
