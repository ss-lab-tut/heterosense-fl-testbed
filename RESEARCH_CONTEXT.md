# 高齢者見守りシステムにおける転倒検知モデルの研究

## 研究背景

高齢者施設では転倒事故が重大な問題となっています。

カメラによる監視は高い性能が期待できる一方で、プライバシーの問題から導入が難しい施設も多くあります。

そこで本研究では、LiDARとベッド圧センサを利用し、プライバシーを保護しながら高精度な転倒検知を実現することを目的とします。

---

## 現在までの研究

これまでの研究では、LiDAR点群から特徴量を抽出し、CNNによる転倒検知モデルを構築してきました。

特徴量として主に以下を利用しています。

- `z_mean`
- `z_std`
- `point_count`
- baselineとの差分
- `pressure`
- `x_std`
- `y_std`
- `xy_spread`
- `z_min`
- `z_max`
- `floor02_ratio`
- `floor04_ratio`

データ補強（Hard Negative）の導入により誤報は減少しましたが、一方でRecallも低下することが確認されました。

つまり、

> 「誤報を減らすと見逃しが増える」

というトレードオフが存在します。

この問題はデータ補強だけでは根本的に解決できません。

---

## 新しい研究テーマ

本研究では、

> 「現在の観測条件を推定し、その条件に応じて転倒判定を切り替える」

という二段階構成を提案します。

研究質問は以下です。

> 観測条件（設置ジオメトリ、遮蔽、センサー可用性）を観測統計から推定し、その条件に応じて転倒検知を切り替えることで、データ補強や設置改善だけでは達成できなかったRecallとFalse Alertsのトレードオフを改善できるか。

---

## 条件推定器

条件推定器の入力には、現実で取得できる統計量のみを使用します。

利用候補：

- `point_count`
- 可視点比率
- `z_mean`
- `z_std`
- baselineとの差分
- `pressure`
- pressureの有無
- `x_std`
- `y_std`
- `xy_spread`
- `floor02_ratio`
- `floor04_ratio`

使用禁止：

- シミュレータ内部パラメータ
- 設置ラベル
- 遮蔽ラベル
- 推論時に取得できない情報

---

## 比較対象

### ベースライン

#### B1

素のCNN＋共通閾値

#### B2

補強CNN＋共通閾値

#### B3

補強CNN＋施設別閾値

#### B4

天井設置＋単純CNN

### 提案手法

#### P1

条件推定器＋動的閾値

#### P2

条件推定結果をCNNへ入力（条件埋め込みまたは条件別ヘッド）

---

## 評価

Accuracyを主指標としません。

主な評価項目：

- Recall
- False Alerts
- Recall–False Alertsトレードオフ
- 最悪条件でのRecall

評価条件：

- `WALL_OCCLUDED` held-out
- LiDARのみ角部屋
- harsh施設
- 遮蔽下通常行動
- 天井視野端

すべて5 seedで実験し、平均・標準偏差を報告します。

---

## 天井設置

天井設置によって壁遮蔽は軽減できる可能性があります。

しかし、以下のような新たな課題も発生します。

- 視野端での点群疎化
- 家具下の影
- 真上視点での姿勢識別
- 圧力センサ有無の異種性

天井シミュレーションは、実LiDARで検証するまで暫定結果として扱います。

---

## 3D LiDAR

3D LiDAR導入後には、以下を検証します。

1. 条件推定器が実点群でも有効か
2. 天井シミュレーションが現実と一致するか

現在の研究は、そのSim-to-Real検証の土台となることを目的としています。
